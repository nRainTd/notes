1. 执行之前不需要执行 `git init` 命令。
2. 会在当前目录下新建一个项目目录，git 仓库位于那个新目录内，而不是当前目录。
3. 使用 HTTPS 可以不用 folk 就直接 `clone` github 上的开源仓库，并且不用进行身份验证。
4. 使用 ssh clone 会提示：![[git_clone-251206-150501.png]]
应该是不能直接 clone 的。
5. 貌似只能 `clone` github 仓库的 main 分支。
